// **Boolean Conversion:**
// Write code to convert the following values to Boolean using `Boolean()`:
// - An empty string (`''`).
// - A non-empty string (`'hello'`).
// - The number `0`.  
// Print the results.

let a=Boolean('');
let b=Boolean("hello")
let c=Boolean(0);
console.log(a);//false   
console.log(b);//true
console.log(c);//false

